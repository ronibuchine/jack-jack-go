


class floogle {
    constructor floogle new(int a, char b) {
        let bar = "baz";
        let floo = true;
    }
    // comment
    function void bar(int a, char b) {
        let i = 42;
        if (i > 17) {
            let blarb = "hello";
            let world = "world";
        }
    }
}
